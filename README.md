Installation using `spring init`

```bash
spring init --name=springbootmvc-jpa-relationships --packageName=demo.jpa.relationships --dependencies=web,h2,thymeleaf,data-jpa springbootmvc-jpa-relationships
```

